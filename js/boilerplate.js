// filename: boilerplate.js

define([
    // these are the path aliases configured in the entry point
    'jquery',
    'underscore',
    'bootstrap',
    'backbone'
], function($, _, Backbone){
    // above, we passed in jQuery, Underscore and Backbone
    // they are not accessible in the global scope

    return{};

});
